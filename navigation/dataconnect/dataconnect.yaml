specVersion: "v1beta"
serviceId: "navigation"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "navigation-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
